# Data Model

> Authoritative entity reference for the Platform. Field types match the Postgres migrations under [`supabase/migrations/`](../../supabase/migrations/). Every table is **RLS-enabled** with the policy outline in [`AUTH-AND-RLS.md`](AUTH-AND-RLS.md).

## 1. Conventions

- Primary keys: `uuid` generated by `gen_random_uuid()`, column `id`.
- Timestamps: `created_at timestamptz not null default now()`, `updated_at timestamptz not null default now()` (kept in sync by a trigger).
- Soft delete: `deleted_at timestamptz null`. No row is ever hard-deleted from the catalog or sales tables.
- Money: integer cents. Column type `bigint`. Column suffix `_cents`. Currency lives in a sibling `currency` column (`USD` at launch).
- Identifiers from external systems (Stripe, suppliers) live in dedicated columns prefixed with the provider (`stripe_session_id`, `supplier_external_id`).
- Enum-like columns use Postgres `enum` types declared in the first migration that needs them.

## 2. Identity & accounts

### `users`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` | mirrors `auth.users.id` from Supabase Auth |
| `email` | `text` | unique, lowercased on insert |
| `display_name` | `text` ||
| `role` | `enum('customer','staff','admin')` | default `'customer'` |
| `phone` | `text` | optional |
| `created_at`, `updated_at`, `deleted_at` | timestamptz ||

### `accounts`
A buying entity. A user can belong to several accounts (e.g. their personal account and an account for their retail store).

| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `display_name` | `text` ||
| `type` | `enum('individual','business')` ||
| `legal_name` | `text` | for businesses |
| `tax_id` | `text` | EIN / VAT / CNPJ where applicable |
| `tier_id` | `uuid` | FK → `tiers.id`; current tier; cached for fast UI |
| `cumulative_units_last_window` | `int` | denormalized counter for tier calc |

### `account_memberships`
| Column | Type | Notes |
|---|---|---|
| `account_id` | `uuid` | FK → `accounts.id` |
| `user_id` | `uuid` | FK → `users.id` |
| `role` | `enum('owner','buyer','viewer')` ||
| primary key | `(account_id, user_id)` ||

## 3. Pricing & tiers

### `tiers`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `code` | `text` | `'tier_1'`, `'tier_2'`, `'tier_3'`, `'tier_4'`; unique |
| `label` | `text` | "Consumer", "Retailer", "Multi-Store", "Wholesale" |
| `min_units` | `int` | 1, 10, 50, 401 |
| `max_units` | `int` | 10, 50, 400, _null_ for unbounded |
| `position` | `int` | sort order |

### `price_rules`
A composable rule. The pricing engine evaluates the matching set per cart line.

| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `scope` | `enum('global','product','variant')` ||
| `target_id` | `uuid` | nullable for `global` |
| `tier_id` | `uuid` | FK → `tiers.id` |
| `kind` | `enum('absolute','percentage_off','margin_floor')` ||
| `value_cents` | `bigint` | for `absolute` |
| `value_bps` | `int` | basis points for `percentage_off` |
| `priority` | `int` | higher wins on conflict |
| `effective_from`, `effective_to` | `timestamptz` ||

### `prices`
Materialized effective prices per variant × tier, refreshed when `price_rules` change.

| Column | Type | Notes |
|---|---|---|
| `variant_id` | `uuid` ||
| `tier_id` | `uuid` ||
| `price_cents` | `bigint` ||
| `currency` | `text` | `'USD'` |
| primary key | `(variant_id, tier_id)` ||

## 4. Catalog

### `products`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `slug` | `text` | unique, URL-safe |
| `brand` | `text` | `'Apple'`, `'Samsung'`, … |
| `model` | `text` | `'iPhone 15 Pro Max'` |
| `summary` | `text` ||
| `description` | `text` | markdown |
| `hero_image_path` | `text` | Supabase Storage path |
| `viewer_3d_path` | `text` | nullable; for graceful fallback |
| `status` | `enum('draft','published','archived')` ||
| `created_at`, `updated_at`, `deleted_at` | timestamptz ||

### `product_variants`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `product_id` | `uuid` ||
| `sku` | `text` | unique |
| `color` | `text` ||
| `storage_gb` | `int` | nullable |
| `condition` | `enum('new','cpo','refurbished','used_a','used_b','used_c')` ||
| `attributes` | `jsonb` | open bag for source-specific extras |

### `inventory_snapshots`
Each supplier sync writes a row per variant. The latest snapshot per (`variant_id`, `supplier_id`) is the source of truth for stock display.

| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `variant_id` | `uuid` ||
| `supplier_id` | `uuid` ||
| `available_qty` | `int` | clamped to ≥ 0 |
| `unit_cost_cents` | `bigint` | what the supplier charges us |
| `currency` | `text` ||
| `as_of` | `timestamptz` ||
| `raw` | `jsonb` | full supplier response for forensic use |

## 5. Suppliers

### `suppliers`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `code` | `text` | `'source-1'`, `'source-2'` |
| `display_name` | `text` ||
| `country` | `text` | `'US'`, `'AE'` |
| `kind` | `enum('dropship','wholesale')` ||
| `is_active` | `boolean` ||
| `notes` | `text` ||

### `supplier_sync_runs`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `supplier_id` | `uuid` ||
| `kind` | `enum('catalog','inventory','prices')` ||
| `started_at`, `ended_at` | `timestamptz` ||
| `rows_seen`, `rows_upserted`, `rows_failed` | `int` ||
| `status` | `enum('running','success','failed','partial')` ||
| `error_summary` | `text` | nullable |

### `inventory_locations`
**The masking map.** Projects an upstream supplier as an OMP-branded location. Admin-only; the `supplier_id` join key never crosses the partner boundary.

| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `supplier_id` | `uuid` | FK → `suppliers.id`; unique |
| `code` | `text` | `'us-tx'`; unique; what partners see |
| `label` | `text` | `'Texas Inventory'` |
| `is_public` | `boolean` | false hides the location's stock from all feeds |

## 6. Partner distribution (outbound feed)

> Tables backing [`PARTNER-INVENTORY-API.md`](PARTNER-INVENTORY-API.md). Consumers are existing `accounts` — no separate tenant entity.

### `partner_api_keys`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `account_id` | `uuid` | FK → `accounts.id` |
| `key_id` | `text` | public half, unique; the only half ever logged |
| `secret_hash` | `text` | Argon2id; plaintext shown exactly once at creation |
| `label` | `text` | human name, e.g. "prod integration" |
| `scopes` | `text[]` | v1: `{'inventory:read'}` only |
| `last_used_at` | `timestamptz` | nullable |
| `expires_at` | `timestamptz` | nullable |
| `revoked_at` | `timestamptz` | nullable; revocation is immediate, never soft-deferred |

### `partner_feed_subscriptions`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `account_id` | `uuid` | FK → `accounts.id` |
| `endpoint_url` | `text` | HTTPS only; validated against an SSRF deny-list on write |
| `signing_secret` | `text` | HMAC key; `Restricted` class; rotates with an overlap window |
| `signing_secret_previous` | `text` | nullable; valid until `secret_rotated_until` |
| `secret_rotated_until` | `timestamptz` | nullable |
| `status` | `enum('active','paused','degraded')` ||
| `last_sequence` | `bigint` | monotonic counter handed to each emitted event |
| `filters` | `jsonb` | optional brand / condition / location subset |

### `partner_margin_rules`
Same composable shape as `price_rules`, deliberately a separate table — retail tier pricing and partner margin must not be conflated.

| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `account_id` | `uuid` | **nullable — null = platform default** |
| `scope` | `enum('global','product','variant')` ||
| `target_id` | `uuid` | nullable for `global` |
| `margin_bps` | `int` | basis points over supplier cost; `check (margin_bps >= 0)` |
| `min_margin_cents` | `bigint` | absolute floor; default `1` |
| `priority` | `int` | higher wins on conflict |
| `effective_from`, `effective_to` | `timestamptz` ||

### `partner_inventory_projection`
Materialized per-partner view of sellable stock. Always **recomputed**, never incremented in place.

| Column | Type | Notes |
|---|---|---|
| `account_id` | `uuid` ||
| `variant_id` | `uuid` ||
| `available_qty` | `int` | net of reservations; `check (available_qty >= 0)` |
| `partner_price_cents` | `bigint` | `ceil(unit_cost_cents * (10000 + margin_bps) / 10000)` |
| `currency` | `text` | `'USD'` |
| `location_id` | `uuid` | FK → `inventory_locations.id` |
| `status` | `enum('available','out_of_stock','delisted')` ||
| `content_hash` | `text` | hash of the partner-visible fields; drives no-op suppression |
| `last_emitted_sequence` | `bigint` | nullable until first delivery |
| `computed_at` | `timestamptz` ||
| primary key | `(account_id, variant_id)` ||

### `partner_webhook_deliveries`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `subscription_id` | `uuid` | FK → `partner_feed_subscriptions.id` |
| `event_id` | `text` | unique; partner-facing idempotency key |
| `sequence` | `bigint` ||
| `payload` | `jsonb` | exactly what was signed and sent |
| `attempt` | `int` | 1-based |
| `http_status` | `int` | nullable on timeout |
| `error` | `text` | nullable |
| `delivered_at` | `timestamptz` | nullable |
| `next_retry_at` | `timestamptz` | nullable; null + no `delivered_at` = terminal failure |

## 7. Sales

### `carts`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `account_id` | `uuid` ||
| `created_at`, `updated_at` | timestamptz ||
| `effective_tier_id` | `uuid` | computed snapshot; refreshed on edit |

### `cart_items`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `cart_id` | `uuid` ||
| `variant_id` | `uuid` ||
| `qty` | `int` | strictly ≥ 1 |
| `unit_price_cents` | `bigint` | snapshot at add-time; refreshed by pricing engine |

### `orders`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `account_id` | `uuid` ||
| `placed_by_user_id` | `uuid` ||
| `status` | `enum('draft','pending_payment','paid','fulfilling','shipped','delivered','canceled','refunded')` ||
| `tier_id_at_order` | `uuid` | tier in force when the order was placed |
| `subtotal_cents`, `tax_cents`, `shipping_cents`, `total_cents` | `bigint` ||
| `currency` | `text` ||
| `stripe_session_id`, `stripe_payment_intent_id` | `text` | nullable until checkout |
| `notes` | `text` | admin-only |
| `created_at`, `updated_at` | timestamptz ||

### `order_items`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `order_id` | `uuid` ||
| `variant_id` | `uuid` ||
| `qty` | `int` ||
| `unit_price_cents` | `bigint` | snapshot at order time |
| `supplier_id` | `uuid` | which feed will fulfill |

### `shipments`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `order_id` | `uuid` ||
| `carrier` | `text` ||
| `tracking_number` | `text` ||
| `status` | `enum('queued','in_transit','delivered','exception','returned')` ||
| `last_event_at` | `timestamptz` ||

### `payments`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `order_id` | `uuid` ||
| `stripe_payment_intent_id` | `text` ||
| `amount_cents` | `bigint` ||
| `currency` | `text` ||
| `status` | `enum('pending','succeeded','refunded','failed','partial_refund')` ||
| `raw_event` | `jsonb` | last received webhook payload |

## 8. Engagement

### `tickets`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `account_id` | `uuid` ||
| `subject` | `text` ||
| `status` | `enum('open','pending','resolved','closed')` ||
| `assignee_user_id` | `uuid` | nullable |

### `ticket_messages`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `ticket_id` | `uuid` ||
| `author_user_id` | `uuid` | nullable when AI-drafted |
| `author_kind` | `enum('customer','staff','ai_draft')` ||
| `body` | `text` ||
| `attachments` | `jsonb` ||

## 9. AI & audit

### `ai_actions`
Every AI proposal lands here. The platform applies an action only after the admin approves it.

| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `agent_code` | `text` | `'pricing-agent'`, … |
| `proposed_by_run_id` | `uuid` ||
| `target_table` | `text` ||
| `target_id` | `uuid` ||
| `proposal` | `jsonb` | structured action + args |
| `rationale` | `text` ||
| `rollback_metadata` | `jsonb` ||
| `status` | `enum('proposed','approved','applied','rejected','rolled_back')` ||
| `approved_by_user_id` | `uuid` | nullable |
| `applied_at` | `timestamptz` ||

### `audit_log`
| Column | Type | Notes |
|---|---|---|
| `id` | `uuid` ||
| `actor_user_id` | `uuid` | nullable for system actions |
| `actor_kind` | `enum('user','system','ai_agent')` ||
| `action` | `text` | dotted-path, e.g. `order.refund` |
| `target_table` | `text` ||
| `target_id` | `uuid` ||
| `before` | `jsonb` ||
| `after` | `jsonb` ||
| `ip` | `inet` ||
| `user_agent` | `text` ||
| `created_at` | `timestamptz` ||

## 10. Indexes & invariants

- `unique (slug)` on `products`, `unique (sku)` on `product_variants`.
- `unique (stripe_payment_intent_id)` on `payments`.
- `check (qty >= 1)` on `cart_items` and `order_items`.
- `check (price_cents >= 0)` on `prices`, `cart_items`, `order_items`.
- `check (available_qty >= 0)` on `inventory_snapshots`.
- Partial index `idx_orders_active on orders (account_id) where status not in ('canceled','refunded')`.
- BRIN index on `audit_log.created_at` for time-window queries.
- `unique (key_id)` on `partner_api_keys`; `unique (event_id)` on `partner_webhook_deliveries`; `unique (supplier_id)` and `unique (code)` on `inventory_locations`.
- `check (margin_bps >= 0)` on `partner_margin_rules`; `check (min_margin_cents >= 1)`.
- `check (available_qty >= 0)` and `check (partner_price_cents >= 1)` on `partner_inventory_projection`.
- **Margin floor invariant:** `partner_price_cents >= unit_cost_cents + min_margin_cents`. Enforced when the rule is written, not when the event is emitted — a rule that would sell at or below cost is rejected at the source.
- Partial index `idx_partner_deliveries_pending on partner_webhook_deliveries (next_retry_at) where delivered_at is null`.

## 11. Migrations index

| # | File | Purpose |
|---|---|---|
| 0001 | `0001_initial_schema.sql` | Enums, identity, accounts, tiers, catalog, suppliers, sales skeleton. |
| 0002 | `0002_rls_policies.sql` | RLS enabled on every table; policies per role. |
| 0003 | `0003_pricing_tiers.sql` | `price_rules`, `prices`, materialization function. |
| 0004 | `0004_supplier_sync.sql` | `inventory_snapshots`, `supplier_sync_runs`, advisory-lock helpers. |
| 0005 | `0005_audit_log.sql` | `audit_log`, `ai_actions`, BRIN index. |
| 0006 | `0006_partner_feed.sql` | `inventory_locations`, `partner_api_keys`, `partner_feed_subscriptions`, `partner_margin_rules`, `partner_inventory_projection`, `partner_webhook_deliveries`, projection recompute function, RLS policies for all six. |
